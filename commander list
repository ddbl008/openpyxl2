pip install -i  https://mirrors.aliyun.com/pypi/simple/ openpyxl

https://blog.csdn.net/ahilll/article/details/81531587 Tk教程

骚技巧：
三元表达式 print a if a>0 else print b 此方法可以用来赋值

with as 可以视作是 try fallaly的另外一种写作

《一行 Python 代码能实现什么丧心病狂的功能？》列表推导式

列表操作 ，使用-1即可将列表翻转

https://www.zhangshengrong.com/p/ArXGbZ5Njy/ 装饰器作用

 python查看支持whl版本
 Amd64
import pip._internal
print(pip._internal.pep425tags.get_supported())
win32
import pip
print(pip.pep425tags.get_supported())

ctrl+shift+F7 查看函数在哪应用
ctrl+shif+A 快捷查询，pycharm siri
ctrl+alt+shift+I python inspection
alt+shift+F10 debug dropdown


#######可能用到的#########
错误捕捉
            try:
                line = next(op)
            except StopIteration as e:
                break
            else: